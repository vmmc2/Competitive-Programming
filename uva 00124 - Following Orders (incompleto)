#include <bits/stdc++.h>

using namespace std;

int visitados[30];
vector <int> adjlist[30];
map <char, int> dictionary;

void preprocess(){
    for(int i = 0; i < 30; i++){
        adjlist[i].clear();
    }
}


int main(){
    //fast Input-Output
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    //Comecou a brincadeira rs
    int a, b;
    string vertices;
    string relations;
    while(getline(cin, vertices)){
        cout << vertices << endl;
        for(int i = 0; i <= (int)vertices.length() - 1; i = i + 2){
            //cout << "vertices[i]: " << vertices[i] << endl;
            dictionary[vertices[i]] = 1;
        }
        
        getline(cin, relations);
        for(int i = 0; i <= (int)relations.length() - 3; i = i + 4){
            a = (int)relations[i] - 97;
            b = (int)relations[i + 2] - 97;
            adjlist[a].push_back(b);
        }
        for(map<char,int>::iterator it = dictionary.begin(); it != dictionary.end(); ++it){
            
        }
    }

    
    return 0;
}
