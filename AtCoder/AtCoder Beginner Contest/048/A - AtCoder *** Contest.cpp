#include <bits/stdc++.h>

using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    
    string a, b, c;
    
    cin >> a >> b >> c;
    
    cout << a[0] << b[0] << c[0] << endl;
    
    return 0;
}
