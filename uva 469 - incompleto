#include <bits/stdc++.h>
#include <string.h>

using namespace std;

char grid[110][110];
char auxiliar[110];
int visitados[110][110];
int xp, yp;

void floodfill(int lin, int col){
    
}

int main(){
    int numcasos;
    scanf("%d", &numcasos);
    while(numcasos--){
        while(true){
            scanf(" %[^\n]", auxiliar);
            
        }
     
        memset(visitados, 0, sizeof visitados);
    }
    return 0;
}
